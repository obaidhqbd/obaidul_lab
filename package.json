{
  "name": "obaidul-mentor-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Static GitHub Pages premium student support lab for HTML and CSS classes.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/clean.mjs",
    "verify": "node scripts/verify-build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
